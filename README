Use GDML from the other CAD to GDML repo here (make sure to place all the files into the base directory with the exampleB3a executable).


TODO: Fix TIR (total internal reflection) and add the specular/diffuse reflection data that Dr.Lang just forwarded.
